#!/bin/bash

docker-compose start netcicd-db keycloak
echo "Waiting for Keycloak to come online"
until $(curl --output /dev/null --silent --head --fail http://keycloak:8080); do
    printf '.'
    sleep 5
done
docker-compose start 